Use `0_rules.md`.

TASK

Generate a reusable component macro.

Requirements:
- File path: `src/_includes/components/{component}.njk`
- Prefix must be `c-`
- Macro signature (if macro style is used):
  `{% macro componentName(props) %}`
- Use placeholder content.

Return only the njk component file.
